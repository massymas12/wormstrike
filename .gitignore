__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
uv.lock

# Real config with credentials — commit the example, not the real one
config.toml

# Built test stubs — generated by build_stubs.py, not stored in source
installers/
